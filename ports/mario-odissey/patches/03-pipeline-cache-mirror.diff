Patch 3 — espelho MGD no slow path + warmup + persistência
==============================================================
Arquivo: src/video_core/renderer_vulkan/vk_pipeline_cache.h
1) Após: #include "video_core/shader_cache.h"
   Adicionar:
     #include "../../mgd/painter/shader/ShaderCache.h"
2) Nos membros privados, após: GraphicsPipelineCacheKey graphics_key{};
   Adicionar:
     // MGD: espelho do cache de pipelines (hit/miss sem mudar comportamento)
     mgd::shader::ShaderCache mgd_shader_cache{8192};

Arquivo: src/video_core/renderer_vulkan/vk_pipeline_cache.cpp
3) Em CurrentGraphicsPipelineSlowPath(), antes do try_emplace, adicionar:
     // MGD: espelho por chave compacta (AssetID/PolygonID do hash). Miss = compilou agora.
     const size_t mgd_h = graphics_key.Hash();
     const mgd::shader::ShaderKey mgd_key{
         static_cast<mgd::AssetID>(mgd_h & 0xFFFFFFFFu),
         static_cast<mgd::PolygonID>((mgd_h >> 32) & 0xFFFFFFFFu), 0};
     if (!mgd_shader_cache.lookup(mgd_key)) {
         mgd_shader_cache.store(mgd_key, 1, mgd_h);
     }
4) Em LoadDiskResources(), após:
       pipeline_cache_filename = base_dir / "vulkan.bin";
   Adicionar:
     // MGD: carrega cache compacto persistido (segunda abertura não recompila)
     mgd_shader_cache.load((base_dir / "mgd_shaders.bin").string());
5) Em LoadDiskResources(), no worker load_graphics, dentro do
   `if (pipeline)` após o emplace, adicionar warmup:
     // MGD warmup: pipeline vindo do disco já entra pronto no cache
     // compacto, antes do primeiro frame (anti-stutter).
     const size_t mgd_h = key.Hash();
     mgd_shader_cache.store(
         mgd::shader::ShaderKey{
             static_cast<mgd::AssetID>(mgd_h & 0xFFFFFFFFu),
             static_cast<mgd::PolygonID>((mgd_h >> 32) & 0xFFFFFFFFu), 0},
         1, mgd_h);
6) Em LoadDiskResources(), junto do SerializeVulkanPipelineCache final, adicionar:
     // MGD: persiste cache compacto junto (perm entre sessões)
     mgd_shader_cache.save((base_dir / "mgd_shaders.bin").string());

Arquivo: src/video_core/CMakeLists.txt
7) Após `add_library(video_core STATIC`, adicionar fontes do MGD:
     ../mgd/query/RegionPolygonCache.cpp
     ../mgd/query/PolygonConsultant.cpp
     ../mgd/mental_map/MentalMap.cpp
     ../mgd/scanner/AssetRegistry.cpp
     ../mgd/scanner/FileDiscovery.cpp

Dependência: fix EventManager sem RTTI (typeid -> static tag), pois o
Eden compila com -fno-rtti (commit 4834e73 no MGD).
